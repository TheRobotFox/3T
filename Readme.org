* Module
  - Translation Unit
  - Has Symbol Table for Linkage
  - Has Own Memory???
  - Data Modell
* Expr
  - Building Block of Structure
** Basic
   Num,Int,String,Char ...
** Callable
   Callable has Environment which inlcudes its arguments
   Arg Pattern is matched against by Interpreter and Environment bindings for args are overwritten
   
  - Closure :: All arguments are evaluated before passed into Environment
    returns expression
  - Macro :: Aguments are lifted into cons before passed into Environment
    returned Form is evaluated
* Type Hierachie
  - GC fixed spacing/ padding (max of types)?
  - Both Untyped Memory and Compact storage -> compatible
* Interpreter
  - Evaluation Context
  - provides exceution context to GC
  - CPS conversion??
* Optimizations
  - Byte-Compile
  - Eval Const Expressions and Inline Macros -> when not dependent on value of arguments
  - Use Lists insead of Concells for code
  - Expressions -> use polymorphism with GC +Arena+ allocation
    Compacting Heap without Alignment  is dangerous when overwriting witth larger objects
    Prob better suited for immutable lisp
    
  - Only have global Scope and local scope overlays (as vectors?)
    
* Memory Sections
  Employ different Memory sections to aid Garbage Collection

  Idealy Memory is relocatable, aim for mark/sweep
  Pointers need to be visible to GC
  
** Module
   Storage page for Loaded [[Module]]s, which includes
   - Static Data (Cons, strings)
   - Byte Code
   - Symbols

   Constant Memory Section, will not be changed after creation and be
  freed upon module unload

   (Section will be loaded on restart no relocation required)
** Cons
   Storage for Cons cells

   In most cases Cons cells will be in a hierachy List/Tree like
   structure => Maybe Group cons cells by dependence eg. free whole
   page Either create runtime overhead or employ partial gc merging
   cons into blocks, can be done semi asyn
** Stack
   Callframes, function stacks

   - Reclaim as much memory as possible while not impacting runtime performance
   - used by byte compiler
* Stages
** Readtime
   Input: Char Stream
   Output: Sexp

   [a-z...] -> Symbol
   ' -> Quoted
   [0-9]+,"..." -> Literals
   Cons -> Cons

   + other reader shit
** Expansion Time
   Input: Sexp
   Output: 3T Expr

   Symbol -> Reference, Callable
   Literal -> Literal
   Cons -> Cons, Macro expansion
** Interpreter
   Input: 3T Expr
   Output: Sexp
** Compiler
   Input: 3T Expr
   Output: ByteCode
* Progress
- Implement special Forms first -> loop, map, if ...
- Implement read-delim as lisp fn


* Immutabillity
  * Spoof Cons with faster iterators
  * Compact Storage in GC
  * More Optimazations


* Memory
  - Heap / Dynamic Storage
    Computation Results
  - Stack ::
    * Argument Storage index based
    * Keep stack of Framesizes
    * Tail-Call-Optimization -> Overwrite Stack Frame 
    * Closure construction ::
      * Precompute which args could end up in environment, alloc on heap
    * Closures register reachable args -> when lambda is executed capture on stack occures -> args get copied to heap and replaced by reference -> env is a list of pointers into heap
    * Rest Args ::
      - build new Cons-List referencing Stack (on Stack)
  - Stack Alternative ::
    * Stack only has pointers to heap allocated args -> more load on GC but lambdas are free -> currying :)
    * Additionally for currying remove stack frames -> always pointer inc/dec -> no &rest args
	
* TODO Progress
  * [ ] GC
  * [ ] Tail-Call-Optimization
  * [ ] Stack-Trace
  * [ ] Step-Through-Debugger
  * [ ] Byte-Compiler
